name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  release_policy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate canonical docs
        run: make docs-validate

      - name: Validate release metadata and workflow policy
        run: make release-validate

      - name: Test release workflow policy
        run: make test-release-policy

      - name: Test development tooling and evidence gates
        run: make test-tooling

  build:
    runs-on: macos-15
    env:
      HOMEBREW_NO_AUTO_UPDATE: "1"
      HOMEBREW_NO_INSTALL_CLEANUP: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "16.0"

      - name: Test Markdown renderer
        run: npm ci --prefix Tools/MarkdownRenderer && npm test --prefix Tools/MarkdownRenderer

      - name: Build
        run: make build

  unit_tests:
    runs-on: macos-15
    env:
      HOMEBREW_NO_AUTO_UPDATE: "1"
      HOMEBREW_NO_INSTALL_CLEANUP: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "16.0"

      - name: Unit Tests
        run: make test-unit

      - name: Upload Unit Test Log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-log-${{ github.run_id }}
          path: logs/test-unit.log
          if-no-files-found: warn

  strict_concurrency:
    runs-on: macos-15
    env:
      HOMEBREW_NO_AUTO_UPDATE: "1"
      HOMEBREW_NO_INSTALL_CLEANUP: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "16.0"

      - name: Strict Concurrency Tests
        run: make test-strict

      - name: Upload Strict Concurrency Log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: strict-concurrency-test-log-${{ github.run_id }}
          path: logs/strict-concurrency-test.log
          if-no-files-found: warn
