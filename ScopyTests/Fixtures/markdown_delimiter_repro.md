# Markdown 公式与加粗：复现样本

## 1. 中文标点与加粗

这是**重点。**请注意：句号留在加粗内容内。

请看**“重要结论”**并继续阅读。

这是**（关键条件）**需要满足。

这是**$E=mc^2$**对应的公式；两侧紧贴中文。

## 2. 公式定界符

单美元公式：$x_i+y_j$；大写变量：$NPV$。

反斜杠公式：\(\frac{a}{b}\)。

| 场景 | 结果 |
| --- | --- |
| 表格中的绝对值 | $\lvert x\rvert<1$ |

## 3. 金额和代码保持原样

价格 $5 和 $10；折扣从 $19.99 到 $9.99。

代码：`**粗体** $x$`，路径：`/tmp/$HOME/file`。

以下是语法错误的对照，仍应显示原文：`** 加粗 **`、`$ x $`、未闭合的 `$x`。
