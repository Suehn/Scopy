import AppKit
import SwiftUI

/// App entry point.
///
///
/// The unit-test target compiles the app sources directly (without main.swift and ScopyApp.swift),
/// so tests never start the SwiftUI app and its NSApplication.

ScopyApp.main()
