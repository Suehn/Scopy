# v0.44.fix17

## 📌 一页纸总结（What / Why / Result）

### What

- UX/Settings：重做 Settings 前端排版与组件结构，对齐 macOS Ventura+（System Settings）风格；保留现有 **Save/Cancel** 显式事务模型与所有设置逻辑不变。

### Why

- 旧 Settings 虽已具备分栏与分组，但细节上仍偏 “自定义面板”：页面 header 信息密度不足、分组视觉不统一、底部 action bar 依赖手工 padding 容易遮挡内容；整体离 macOS 13+ 系统设置风格还有明显差距。

### Result

- 每页统一为 **Header（图标 + 标题 + 副标题）→ 卡片式分组**，视觉更像系统设置。
- 引入可复用的 Settings 组件（Section/Card/Row/Divider），页面之间一致性更好。
- footer 改用 `safeAreaInset` 固定到底部，减少遮挡与留白问题（仍保留 Save/Cancel/Reset 逻辑）。

---

## 📂 核心改动

- `Scopy/Views/Settings/SettingsView.swift`
  - Detail 内容改为页面自滚动 + `safeAreaInset` footer；侧边栏行样式加入 subtitle 与配色优化。
- `Scopy/Views/Settings/SettingsPageHeader.swift`
  - Header 增加图标 + 副标题，并统一页级容器的 padding/背景。
- `Scopy/Views/Settings/SettingsComponents.swift`
  - 新增 Settings UI 基础组件：`SettingsSection` / `SettingsCard` / `SettingsCardRow` / `SettingsCardDivider`。
- `Scopy/Views/Settings/*SettingsPage.swift`
  - 各页面改用卡片式分组布局（仅 UI 结构变化，绑定与逻辑保持不变）。
- `Scopy.xcodeproj/project.pbxproj`
  - 生成工程以纳入新增 Settings 组件文件。

---

## ✅ 测试

- 单元测试：`make test-unit`：Executed 201 tests, 1 skipped, 0 failures
- UI 测试：本地尝试运行 `ScopyUITests/SettingsUITests`，但当前机器出现 “Timed out while enabling automation mode” 导致 runner 初始化失败（不影响功能逻辑；建议在 CI 或开启 UI automation 的环境复测）。
