# v0.29.1 - P0 准确性：fuzzyPlus ASCII 多词去噪

**日期**: 2025-12-12

---

## 📌 一页纸总结

### What
- 修复 fuzzyPlus 在英文多词查询下出现大量弱相关结果的问题（如文件路径/随机文件名被误召回）。
- 保持全量历史覆盖与渐进首屏体验不变。

### Why
- v0.25 起 fuzzyPlus 采用「每词独立 subsequence 模糊匹配」语义。  
  对 ASCII 长词（≥3）来说，subsequence 过于宽松：只要字符顺序出现即可，导致跨路径/噪音文本也被匹配。
- 用户预期英文多词搜索应更接近“每个词连续出现（contains）”的语义，而非字符散落匹配。

### Result
- fuzzyPlus 对 ASCII 长词改为**必须连续子串匹配**，显著降低弱相关召回；  
  仍支持中文/非 ASCII 词的顺序模糊匹配，保持全量历史零漏召回。

---

## 🏗️ 核心实现

**文件**：`Scopy/Services/SearchService.swift`

- 在 `searchInFullIndex` 的 fuzzyPlus 评分中：
  - 若 `word.isASCII && word.count >= 3`，则要求 `plainTextLower` 中存在该词的连续子串；
  - 未命中则该 item 直接淘汰，不再 fallback 到 subsequence。
- 其它语义保持不变：
  - ASCII 短词（≤2）仍使用连续子串；
  - 非 ASCII（中文等）继续用严格顺序 subsequence + 评分排序。

---

## ✅ 修改文件列表

- `Scopy/Services/SearchService.swift`
- `ScopyTests/SearchServiceTests.swift`
- `doc/implementation/releases/v0.29.1.md`

---

## 🧪 测试 & 性能

- `make test-unit` ✅ **53 tests passed** (1 perf skipped)
- `make test-perf` ✅ **22/22 passed（含重载）**

**性能实测（Apple Silicon, macOS 14, Debug, `make test-perf`）**：
- Fuzzy 5k items P95 ≈ 4.68ms
- Fuzzy 10k items P95 ≈ 43.52ms
- Disk 25k fuzzy P95 ≈ 43.40ms
- Heavy Disk 50k fuzzy P95 ≈ 82.76ms
- Ultra Disk 75k fuzzy P95 ≈ 122.24ms

---

## 🔮 遗留与后续

- 如需进一步提升英文长词“容错模糊”体验，可在 fuzzy（非 plus）模式继续使用 subsequence 语义。

---

**维护者**: Codex CLI  
**最后更新**: 2025-12-12

