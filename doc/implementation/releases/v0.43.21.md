# v0.43.21

## 📌 一页纸总结（What / Why / Result）

### What

- Dev/Release：完善发布自动化，确保 GitHub Release 产物与 Homebrew Cask 的版本/sha 对齐。

### Why

- 近期出现 `brew install --cask scopy` 报 `SHA-256 mismatch`：通常是同一 tag 的 DMG 被重新生成/覆盖，导致 Homebrew 端固定的 sha 与实际下载不一致。

### Result

- `main` 推送且更新实现文档后，可自动从实现文档索引读取版本并打 tag，触发 Release workflow 构建 DMG。
- Release workflow 默认拒绝覆盖同一 tag 已发布的 DMG，避免再次触发 Homebrew SHA mismatch。
- 可选：发布后自动对 `Homebrew/homebrew-cask` 发起 bump PR（需要配置 token），让用户直接 `brew install --cask scopy` 获取最新版。

---

## 🏗️ 实现路线

1. 增加 release-doc 校验脚本，确保“版本文档/索引/CHANGELOG”一致后才允许自动打 tag。
2. 增加 `main` push 的自动打 tag workflow（tag 仍来自实现文档索引）。
3. Release workflow 增加“拒绝覆盖既有 DMG”保护。
4. Release workflow 增加对 `Homebrew/homebrew-cask` 的自动 bump PR（可选、失败不阻断发布）。

---

## 📂 核心改动

- `.github/workflows/auto-tag.yml`
- `.github/workflows/release.yml`
- `scripts/release/validate-release-docs.sh`
- `DEPLOYMENT.md`
- `README.md`

---

## 🎯 关键指标

- 目标：避免同一版本（同一 tag）的发布产物被覆盖，从源头杜绝 Homebrew `SHA-256 mismatch`。
- 目标：用户侧可直接 `brew install --cask scopy` 获取最新版本（通过自动化 PR 减少人工步骤）。

---

## 📊 当前状态

- 单元测试：`make test-unit` **147 passed** (1 skipped)（沿用 v0.43.20）
- Strict Concurrency：`make test-strict` **147 passed** (1 skipped)（沿用 v0.43.20）

---

## 🔮 遗留与后续

- 建议为旧版本线（`v0.18.*`）停止覆盖同名资产；若需修复旧版本，使用新版本号+新 tag 发布。
- 需要在仓库 Secrets 中配置 `HOMEBREW_GITHUB_API_TOKEN`（PAT），才能自动对 `Homebrew/homebrew-cask` 发 PR。

