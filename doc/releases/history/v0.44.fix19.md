# v0.44.fix19

## 📌 一页纸总结（What / Why / Result）

### What

- UX/Settings：修复 Settings Detail（右侧内容区）背景出现“两种颜色”的割裂感，仅前端视觉改动。

### Why

- 部分页面的 Detail 内容未填满容器，导致内容区背景与 NavigationSplitView 默认背景叠加，深色模式下更明显地呈现为“两种颜色”。

### Result

- Detail 内容强制 `frame(maxWidth/Height: .infinity)` 并统一背景为 `windowBackgroundColor`，背景一体性更好。
- Save/Cancel 事务模型与设置逻辑保持不变。

---

## 📂 核心改动

- `Scopy/Views/Settings/SettingsView.swift`
  - Detail 内容视图填满可用空间并统一背景，消除双背景色。

---

## ✅ 测试

- 单元测试：`make test-unit`：Executed 201 tests, 1 skipped, 0 failures

