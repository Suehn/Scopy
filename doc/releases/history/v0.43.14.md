# v0.43.14 — Fix/UX：字数统计按“词/字”单位（中英文混排更准确）

**日期**：2025-12-15

## 📌 一页纸总结（What / Why / Result）

- **What**：修复列表元数据中的“字数”统计：英文/数字按词计数，中文/日文/韩文按字计数（避免把一个英文单词按字母拆成多个“字”）。
- **Why**：之前使用 `text.count` 直接按字符计数，导致英文单词会被按字母计入“字数”，在中英文混排内容下不符合预期。
- **Result**：元数据更接近常见“字数统计”的口径：`serstein 的大 gap` 计为 4（而不是 7+…）。

## 🏗️ 实现路线

1. 抽出可复用的 `TextMetrics.displayWordUnitCount(for:)`（ScopyKit）
2. 在 UI 的 metadata 生成处用该计数替换原 `text.count`
3. 补充单测覆盖中英文混排与 joiner（’）

## 📂 核心改动

- `Scopy/Domain/Utilities/TextMetrics.swift`：新增“字/词”单位计数
- `Scopy/Presentation/ClipboardItemDisplayText.swift`：metadata 使用新计数逻辑
- `ScopyTests/TextMetricsTests.swift`：新增单测

## 🎯 关键指标

- `make test-unit`：**147 passed** (1 skipped)
- Thread Sanitizer：`make test-tsan` **147 passed** (1 skipped)
- Strict Concurrency：`make test-strict` **147 passed** (1 skipped)
- 集成/性能：本版本不涉及搜索/存储热路径，沿用 v0.43.13 基线（`make test-integration` 12 passed；`make test-perf` 17 passed, 6 skipped）

## 📊 当前状态（快速检查）

- Debug build：✅
- 测试：✅（unit/tsan/strict）

## 🔮 遗留与后续

- 如果希望中文“词”级别分词（而非按字），可考虑引入更重的分词器；当前实现选择与性能/一致性更友好的“按字 + 英文按词”口径。

