# v0.36.1 性能对比（v0.36 → v0.36.1）

**日期**: 2025-12-13

## 对比版本

- v0.36 → v0.36.1

## 测试环境

- 设备：Apple M3
- 系统：macOS 15.7.2（24G325）
- 架构：arm64
- 配置：Debug
- 命令：`make test-perf`（`RUN_PERF_TESTS=1`）
- 备注：heavy 场景需 `RUN_HEAVY_PERF_TESTS=1` 才运行

## 性能对比表格

> 说明：v0.36.1 主要为 Thread Sanitizer（Hosted tests）回归方案与测试基础设施改动；不触及 Search/Storage 核心性能路径，本表沿用 v0.36 基线数值便于索引与回溯。

| 指标 | v0.36 | v0.36.1 |
|------|------:|--------:|
| Fuzzy 5k items P95 | 5.23ms | 5.23ms（沿用基线） |
| Fuzzy 10k items P95 | 44.80ms | 44.80ms（沿用基线） |
| Disk 25k fuzzy P95 | 56.94ms | 56.94ms（沿用基线） |
| Bulk insert 1000 items | 54.80ms（≈18,248 items/s） | 54.80ms（沿用基线） |
| Fetch recent (50 items) avg | 0.07ms | 0.07ms（沿用基线） |
| Regex 20k items P95 | 3.08ms | 3.08ms（沿用基线） |
| Mixed content disk search（single run, after warmup） | 4.27ms | 4.27ms（沿用基线） |

## 测试用例变化

- 无（测试/工程配置改动，不改 perf 用例）

## 性能回归说明

- 无代码性能路径改动；如出现指标变化，优先排查 run-to-run 波动与系统负载。

## 总结

- v0.36.1 的重点是并发回归门槛（TSan），性能基线沿用 v0.36 记录。

