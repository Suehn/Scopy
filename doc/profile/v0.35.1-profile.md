# v0.35.1 性能对比（v0.35 → v0.35.1）

**日期**: 2025-12-13

## 对比版本

- v0.35 → v0.35.1

## 测试环境

- 设备：Apple M3
- 系统：macOS 15.7.2（24G325）
- 架构：arm64
- 配置：Debug
- 命令：`make test-perf`（`RUN_PERF_TESTS=1`）
- 备注：heavy 场景需 `RUN_HEAVY_PERF_TESTS=1` 才运行

## 性能对比表格

> 说明：v0.35.1 为 **doc-only** 版本（无代码改动），本表用于把 v0.35 的基线数值显式挂接到 v0.35.1，便于索引与回溯。

| 指标 | v0.35 | v0.35.1 |
|------|------:|--------:|
| Fuzzy 5k items P95 | 4.69ms | 4.69ms（沿用基线） |
| Fuzzy 10k items P95 | 44.81ms | 44.81ms（沿用基线） |
| Disk 25k fuzzy P95 | 55.73ms | 55.73ms（沿用基线） |
| Bulk insert 1000 items | 54.33ms（≈18,405 items/s） | 54.33ms（沿用基线） |
| Fetch recent (50 items) avg | 0.07ms | 0.07ms（沿用基线） |
| Regex 20k items P95 | 3.03ms | 3.03ms（沿用基线） |

## 测试用例变化

- 无（doc-only 版本）

## 性能回归说明

- 无代码改动；如出现指标变化，优先排查 run-to-run 波动与系统负载。

## 总结

- v0.35.1 的目标是文档索引/变更对齐，不引入性能变化；性能基线以 v0.35 记录为准。

