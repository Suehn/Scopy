# v0.35.1 - 文档对齐版本：补齐索引/变更/部署/review（基线 v0.35）

**日期**: 2025-12-13

---

## 一页纸总结

### What

- 补齐并对齐 v0.30–v0.35 已完成重构对应的“索引/变更/部署/性能入口”文档：
  - `doc/implemented-doc/README.md` 当前版本与版本导航
  - `doc/implemented-doc/CHANGELOG.md` 新增 v0.30–v0.35 变更条目
  - `doc/profile/README.md` 补齐 v0.32–v0.35 性能记录入口
  - `DEPLOYMENT.md` 更新“本次更新”摘要到 v0.35 基线
  - `doc/review/review-v0.3.md` 基线与阶段状态对齐到 v0.35

### Why

- v0.30–v0.35 的代码重构已落地（Settings SSOT / SQLite boundary / Search actor / ClipboardService actor / cache 收口 / UI 拆分），但索引与变更文档仍停留在 v0.29.1，影响后续协作与稳定性回归。
- 按 `CLAUDE.md`/`AGENTS.md`：每次开发完成必须同步版本文档、索引与 CHANGELOG；性能/部署信息必须带环境与具体数值。

### Result

- 文档与代码版本一致：当前索引/变更/部署/性能记录均对齐到 v0.35 基线。
- **代码无变更**（仅文档整理与对齐），便于后续 Phase 6 收尾继续推进。

---

## 实现路线

1. 更新 `doc/implemented-doc/README.md`：当前版本与快速导航补齐 v0.30–v0.35 + v0.35.1
2. 更新 `doc/implemented-doc/CHANGELOG.md`：补齐 v0.30–v0.35 条目，并新增 v0.35.1（doc-only）
3. 更新 `doc/profile/README.md`：补齐 v0.32–v0.35 的入口，并新增 v0.35.1-profile（沿用基线）
4. 更新 `DEPLOYMENT.md`：将“本次更新”指向 v0.35 基线并写入环境与数值
5. 更新 `doc/review/review-v0.3.md`：基线与阶段完成状态对齐到 v0.35

---

## 核心改动（文件）

- `DEPLOYMENT.md`
- `doc/implemented-doc/README.md`
- `doc/implemented-doc/CHANGELOG.md`
- `doc/profile/README.md`
- `doc/profile/v0.35.1-profile.md`
- `doc/review/review-v0.3.md`

---

## 关键指标

> 本版本不改代码；测试/性能沿用 v0.35 基线记录（详见 `doc/profile/v0.35-profile.md`）。

- 单元测试：`make test-unit` **53 tests passed** (1 skipped)
- 性能测试：`make test-perf` **22 tests passed** (6 skipped；heavy 需 `RUN_HEAVY_PERF_TESTS=1`)

---

## 当前状态（快速检查）

- 当前版本：v0.35.1（doc-only），代码基线：v0.35（`a542326`）
- 关键重构已完成：Phase 0–5（见 `doc/review/review-v0.3.md`）
- 待继续：Phase 6（dead code 清理、日志统一、Strict Concurrency/TSan 回归）

---

## 遗留与后续

- 推进 Phase 6：
  - 统一日志（除热键文件日志外迁入 `os.Logger`）
  - 为 `AsyncStream` 显式指定 buffering policy（monitor/event stream）
  - 清理已不再需要的旧接口/死代码（按 `doc/review/review-v0.3.md` 清单）

